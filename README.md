# js-practise

To install dependencies:

```bash
bun install
```

To run:

```bash
bun --watch index.ts
```

# actions simulate

to simulate github actions locally. we use the `act` package from pacman on our arch linux. command are

```bash
act -W .github/workflows/linter.yml
```
